---
name: project
description: "CEO orchestrator V8 (Eros Brain). Deterministic orchestrator controls Claude via next/done loop. Triggers on 'new project', 'nuevo proyecto', 'crear proyecto', 'start project', '/project', 'train project', 'entrenar'."
user_invocable: true
---

See `.eros/workflows/project.md` for the canonical workflow content.

This stub exists so Claude's `Skill("project")` tool can invoke the workflow. The full content lives in the AI-neutral `.eros/workflows/` directory and is shared across Claude, Gemini, and Codex agents.
